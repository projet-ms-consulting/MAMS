{% extends 'base.html.twig' %}

{% block title %}Expenses{% endblock %}

{% block body %}
    <h1>Expenses</h1>

    <table class="vehicle-table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense.id }}</td>
            </tr>
            <tr>
                <th>ExpenseType</th>
                <td>{{ expense.expenseType }}</td>
            </tr>
            <tr>
                <th>TotalAmount</th>
                <td>{{ expense.TotalAmount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_expenses_index') }}"><button class="btnAppPerso">Retour à la liste</button></a>

    <a href="{{ path('app_expenses_edit', {'id': expense.id}) }}"><button class="btnAppPerso">Modifier</button></a>

    {{ include('expenses/_delete_form.html.twig') }}
{% endblock %}
