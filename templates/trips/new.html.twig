{% extends 'base.html.twig' %}

{% block title %}New Trips{% endblock %}

{% block body %}


    {# Barre de navigation #}
    <nav class="navbar">

        <div class="logo">
            <img src="{{ asset('img/profile.png') }}" alt="Logo">
        </div>

        <ul class="navbar-nav">
            <li class="nav-item">
                <a href="{{ path('app_profile') }}" class="nav-link">Mon profile</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('app_logout') }}" class="nav-link">Déconnexion</a>
            </li>
        </ul>
    </nav>

    <h1>Create new Trips</h1>

    {{ include('trips/_form.html.twig') }}

    <a href="{{ path('app_trips_index') }}">Retour à la liste</a>



{% endblock %}


