{% extends 'base.html.twig' %}

{% block title %}Vehicle index{% endblock %}

{% block body %}
    <h1>Liste des véhicules</h1>

   {# <h2>Véhicule avec trajets</h2>
<div class="table-container">
    <table class="vehicle-table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Marque</th>
            <th>Model</th>
            <th>Immatriculation</th>
            <th>Puissance fiscale</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for vehicle in vehiclesWithTrips %}
            <tr>
                <td>{{ vehicle.id }}</td>
                <td>{{ vehicle.Label }}</td>
                <td>{{ vehicle.model }}</td>
                <td>{{ vehicle.plate }}</td>
                <td>{{ vehicle.fiscalPower }}</td>
                <td>
                    <a href="{{ path('app_vehicle_show', {'id': vehicle.id}) }}">Show</a>
                    <a href="{{ path('app_vehicle_edit', {'id': vehicle.id}) }}">Edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>#}

{#    <h2>Vehicles without trips</h2>#}
    <div class="table-container">
    <table class="vehicle-table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Marque</th>
            <th>Modèle</th>
            <th>Immatriculation</th>
            <th>Puissance fiscale</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for vehicle in vehiclesWithoutTrips %}
            <tr>
                <td>{{ vehicle.id }}</td>
                <td>{{ vehicle.Label }}</td>
                <td>{{ vehicle.model }}</td>
                <td>{{ vehicle.plate }}</td>
                <td>{{ vehicle.fiscalPower }}</td>
                <td>
                    <a href="{{ path('app_vehicle_show', {'id': vehicle.id}) }}">Voir</a>
                    <a href="{{ path('app_vehicle_edit', {'id': vehicle.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Aucun véhicule trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>

    <a href="{{ path('app_vehicle_new') }}"><button class="btnAppPerso">Créer nouveau véhicule</button></a>
{% endblock %}
